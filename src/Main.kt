import java.io.File

fun main (args : Array<String>){
/*  val inputFile = File("input")
    val tabText = inputFile.readLines(Charsets.UTF_8)
    val firstLine = tabText[0].split(" ").map{it.toInt()}
    val numberOfVideos = firstLine[0]

    //Output file :
    val outputFile = File("output.txt")
    outputFile.appendText("some text")
    */
}